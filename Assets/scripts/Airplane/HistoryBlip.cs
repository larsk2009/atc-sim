using UnityEngine;

namespace Assets.scripts.Airplane
{
    public class HistoryBlip : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
